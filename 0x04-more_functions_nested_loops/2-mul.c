#include "main.h"

/**
 * mul - multiplies two integer and return the result
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Return: multiple of a and b
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
