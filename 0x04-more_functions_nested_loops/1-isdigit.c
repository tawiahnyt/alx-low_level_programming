#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: the character to be checked
 * Return: 1 if c is an integer, 0 oherwise
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
